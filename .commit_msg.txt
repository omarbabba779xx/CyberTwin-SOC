fix(security): npm audit clean of all HIGH CVEs in frontend deps

The newly-blocking `npm audit --audit-level=high` gate caught 3 HIGH CVEs
in transitive dependencies of the frontend bundle:

1. d3-color <3.1.0 — ReDoS (GHSA-36jr-mh4h-2g58)
   Pulled in by react-simple-maps@3.0.0 -> d3-zoom -> d3-interpolate -> d3-color.
   Fixed by an `overrides` block forcing d3-color to ^3.1.0.

2. lodash <=4.17.23 — Code Injection via `_.template` (GHSA-r5fr-rjxr-66jc)
   + Prototype Pollution (GHSA-f23m-r3pf-42rh).
   Fixed via `npm audit fix` (non-breaking).

3. picomatch <=2.3.1 || 4.0.0-4.0.3 — Method Injection (GHSA-3v7f-55p6-f55p)
   + ReDoS (GHSA-c2c7-rcm5-vvqj).
   Fixed via `npm audit fix` (non-breaking).

Result: `npm audit --audit-level=high` is clean.
Remaining: 2 moderate (esbuild + vite dev-server only) — flagged for the
next dependency-bump pass; they don't ship to production users.

`npm run build` verified successful (12.15s).
